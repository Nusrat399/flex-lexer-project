# flex-lexer-project
A simple and comprehensive lexer built with Flex to tokenize C and C++ code. This lexer can identify keywords, operators, literals, numbers (including HEX, BINARY, OCTAL, FLOAT, SCIENTIFIC), identifiers, function calls, comments, preprocessor directives, and other symbols. It also includes custom tokens like annotations.
